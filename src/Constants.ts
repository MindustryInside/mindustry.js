export class Constants {
    static readonly DEFAULT_PORT = 6567;
    static readonly DISCOVER_PACKET = Buffer.of(-2, 1);
    static readonly Gamemodes = ['survival', 'sandbox', 'attack', 'pvp', 'editor'];
}
